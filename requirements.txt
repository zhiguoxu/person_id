# ==============================================================================
# 机器人视觉人物识别系统 - 依赖清单
#
# 安装方式 (二选一):
#   1. 使用安装脚本 (推荐):  ./install.sh
#   2. 手动分步安装:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#      pip install -r requirements.txt
# ==============================================================================

# --- 核心检测与追踪 ---
ultralytics>=8.3.0          # YOLO11-pose
boxmot>=11.0.0              # BoT-SORT 追踪器

# --- 人脸识别 ---
insightface>=0.7.3          # SCRFD + ArcFace-R100
onnxruntime-gpu>=1.18.0     # InsightFace GPU 推理

# --- 深度学习基础 (torch/torchvision 需先单独安装, 见 install.sh) ---
opencv-python>=4.9.0
numpy>=1.26.0
scipy>=1.12.0

# --- Web 服务 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
websockets>=12.0

# --- VLM 仲裁 ---
openai>=1.14.0

# --- 持久化 ---
aiosqlite>=0.20.0
sqlmodel>=0.0.22

# --- 工具 ---
pillow>=10.2.0
scikit-learn>=1.4.0
loguru>=0.7.0

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
